#!/usr/bin/python3
def square_matrix_simple(matrix=[]):
    matrix_len = len(matrix)
    for i in range(matrix_len):
        second_len = len.(matrix[i])
        for j in range(second_len):
            new_matrix[i][j] = (matrix[i][j]) * (matrix[i][j])
    return new_matrix
